import ScrollStack from "@/components/common/ScrollStack";
import Hero from "@/modules/hero";

const page = () => {
    return (
        <div>
            <Hero/>
            <ScrollStack/>
        </div>
    );
};

export default page;