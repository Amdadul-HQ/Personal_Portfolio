# ==============================================================================
#  Portfolio Dashboard (admin) — Environment Variables (TEMPLATE)
#  Copy to `.env`:  cp .env.example .env
# ==============================================================================

# Base URL of the Portfolio Backend API, INCLUDING the /api prefix.
# Local dev:        http://localhost:5000/api
# Deployed backend: https://your-backend.vercel.app/api
NEXT_PUBLIC_API_URL=http://localhost:5000/api
